schemaVersion: treeseed.guarantee/v1
id: guarantee.admin.feedback.screenshot-privacy.029
type: admin
subtype: feedback
journey: Feedback Screenshot Privacy
ownerPackage: "@treeseed/admin"
surface: admin-ui
summary: Optional screenshots preserve the complete page and active overlays without the feedback panel, redact private values in the browser, and remain privately stored.
status: active
dependencies: { journeys: [], guarantees: [guarantee.admin.feedback.authenticated-submission.028] }
actors: { allowed: [authenticated_user], forbidden: [anonymous_user] }
devices: { required: [desktop_chromium, tablet_chromium, mobile_chromium] }
gates: [core, security, release]
preconditions: { fixtures: [integrated_treeseed_project], notes: [The feedback submission session is active.] }
scene: { required: true, manifest: ./scenes/submit-feedback.scene.yaml, executionKey: admin.feedback.submit, mode: { acceptance: true, demo: true, training: true }, entryRoute: /app/, componentContract: admin.feedback-management, expectedEvidence: [playwright_trace, screenshot_on_failure, network_log] }
api: { required: true, verifierRefs: [api.feedback.management] }
content: { required: false, verifierRefs: [] }
audit: { required: true, verifierRefs: [api.feedback.management] }
negativeCases: [{ id: attachment-private, actor: non_admin_user, verifierRefs: [api.feedback.management] }]
evidence: { required: [execution_graph, playwright_trace, api_verification_log] }
notes: [This guarantee shares one real submission execution instead of duplicating its UI mutations.]
run: { timeoutSeconds: 600, requiredForRelease: true }
