schemaVersion: treeseed.guarantee/v1
id: guarantee.team.membership.accept-team-invitation.018
journeyIndex: 18
type: team
subtype: membership
journey: Accept Team Invitation
ownerPackage: "@treeseed/admin"
summary: Redeem an invite and attach the user to the team; should handle authenticated and newly registered users cleanly.
status: active
dependencies:
  journeys:
    - 17
  guarantees:
    - guarantee.team.membership.invite-team-members.017
actors:
  allowed:
    - invited_user
  forbidden:
    - mismatched_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 17"
uiFeature:
  contract: ../team-management.ui-feature.yaml
  capabilities: [team.invitation-consent]
scene:
  required: true
  manifest: ./scenes/accept-team-invitation.scene.yaml
  executionKey: admin.team.accept-invite
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /team-invites/[token]/accept
  componentContract: redirect/session service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.team.accept.correlated
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs: [api.team.accept.correlated]
negativeCases:
  - id: mismatched-user-denied
    actor: mismatched_user
    sceneManifest: scenes/invitation-mismatched-user.scene.yaml
    executionKey: admin.team.invitation.mismatched-user
    verifierRefs: [api.team.invitation.mismatch-denied]
  - id: invalid-expired-revoked-replay
    verifierRefs: [api.team.invitation.terminal-states]
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Redeem an invite and attach the user to the team; should handle authenticated and newly registered users cleanly.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
