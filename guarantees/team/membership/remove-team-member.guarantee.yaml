schemaVersion: treeseed.guarantee/v1
id: guarantee.team.membership.remove-team-member.020
journeyIndex: 20
type: team
subtype: membership
journey: Remove Team Member
ownerPackage: "@treeseed/admin"
summary: Remove or deactivate a team member with auditability; must avoid orphaning owned projects decisions or running workdays.
status: active
dependencies:
  journeys:
    - 18
  guarantees:
    - guarantee.team.membership.accept-team-invitation.018
actors:
  allowed:
    - team_admin
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Planned by guarantee journey audit: the referenced UI route/behavior is not implemented as an executable service journey yet.
    - "Journey dependencies must be satisfied: 18"
scene:
  required: true
  manifest: ./scenes/remove-team-member.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/teams/visual-audit-team/members
  componentContract: record-action service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.team.member.remove
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: non-member-denied
    actor: non_member
    verifierRefs:
      - api.team.members.nonmember
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Remove or deactivate a team member with auditability; must avoid orphaning owned projects decisions or running workdays.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
