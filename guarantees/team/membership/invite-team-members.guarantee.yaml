schemaVersion: treeseed.guarantee/v1
id: guarantee.team.membership.invite-team-members.017
journeyIndex: 17
type: team
subtype: membership
journey: Invite Team Members
ownerPackage: "@treeseed/admin"
summary: Create team invitation records and send notices; roles should map to API/admin authorization rather than client-only checks.
status: active
dependencies:
  journeys:
    - 14
  guarantees:
    - guarantee.team.team.edit-team-settings.014
actors:
  allowed:
    - team_admin
    - team_owner
  forbidden:
    - contributor
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 12"
uiFeature:
  contract: ../team-management.ui-feature.yaml
  capabilities: [team.member-directory, team.invitations]
scene:
  required: true
  manifest: ./scenes/invite-team-members.scene.yaml
  executionKey: admin.team.invite
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/teams/[teamId]/members
  componentContract: modal-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.team.invite.correlated
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs: [api.team.invite.correlated]
negativeCases:
  - id: contributor-denied
    actor: contributor
    sceneManifest: ../scenes/negative/members-contributor.scene.yaml
    executionKey: admin.team.negative.members-contributor
    verifierRefs: [api.team.invite.contributor-denied]
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Create team invitation records and send notices; roles should map to API/admin authorization rather than client-only checks.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
