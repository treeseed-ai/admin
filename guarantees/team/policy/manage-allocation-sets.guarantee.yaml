schemaVersion: treeseed.guarantee/v1
id: guarantee.team.policy.manage-allocation-sets.092
journeyIndex: 92
type: team
subtype: policy
journey: Manage Allocation Sets
ownerPackage: "@treeseed/admin"
summary: Define team-level capacity budgets limits and eligibility policies; this is distinct from project or agent allocation and belongs outside providers.
status: active
dependencies:
  journeys:
    - 12
  guarantees:
    - guarantee.team.team.create-team.012
actors:
  allowed:
    - team_admin
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Planned by guarantee journey audit: the referenced UI route/behavior is not implemented as an executable service journey yet.
    - "Journey dependencies must be satisfied: 12"
scene:
  required: true
  manifest: ./scenes/manage-allocation-sets.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/capacity/allocation
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.team.permissions.owner
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: non-member-denied
    actor: non_member
    verifierRefs:
      - api.team.members.nonmember
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Define team-level capacity budgets limits and eligibility policies; this is distinct from project or agent allocation and belongs outside providers.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
