schemaVersion: treeseed.guarantee/v1
id: guarantee.team.team.edit-team-settings.014
journeyIndex: 14
type: team
subtype: team
journey: Edit Team Settings
ownerPackage: "@treeseed/admin"
summary: Update team metadata and policy knobs; should not mutate package or provider infrastructure directly.
status: active
dependencies:
  journeys:
    - 13
  guarantees:
    - guarantee.team.team.switch-active-team.013
actors:
  allowed:
    - team_admin
    - team_owner
  forbidden:
    - contributor
devices:
  required:
    - desktop_chromium
    - tablet_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 12"
uiFeature:
  contract: ../team-management.ui-feature.yaml
  capabilities: [team.identity-settings]
scene:
  required: true
  manifest: ../scenes/edit-team-settings.scene.yaml
  executionKey: admin.team.edit
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/teams/[teamId]/edit
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.team.update.correlated
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs: [api.team.update.correlated]
negativeCases:
  - id: contributor-denied
    actor: contributor
    sceneManifest: ../scenes/negative/edit-contributor.scene.yaml
    verifierRefs: [api.team.update.contributor-denied]
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Update team metadata and policy knobs; should not mutate package or provider infrastructure directly.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
