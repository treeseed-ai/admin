schemaVersion: treeseed.guarantee/v1
id: guarantee.team.team.create-team.012
journeyIndex: 12
type: team
subtype: team
journey: Create Team
ownerPackage: "@treeseed/admin"
summary: Create the team boundary that owns projects capacity policy members and billing-like settings; this should be API-backed durable state.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Planned by guarantee journey audit: the referenced UI route/behavior is not implemented as an executable service journey yet.
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/create-team.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/teams/new
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.team.create
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: non-member-denied
    actor: non_member
    verifierRefs:
      - api.team.members.nonmember
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Create the team boundary that owns projects capacity policy members and billing-like settings; this should be API-backed durable state.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
