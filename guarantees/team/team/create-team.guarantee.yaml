schemaVersion: treeseed.guarantee/v1
id: guarantee.team.team.create-team.012
journeyIndex: 12
type: team
subtype: team
journey: Create Team
ownerPackage: "@treeseed/admin"
summary: Create the durable team identity and membership boundary used by active-team selection and team management.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
devices:
  required:
    - desktop_chromium
    - tablet_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 4"
uiFeature:
  contract: ../team-management.ui-feature.yaml
  capabilities: [team.create]
scene:
  required: true
  manifest: ../scenes/create-team.scene.yaml
  executionKey: admin.team.create
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/teams/new
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.team.create.correlated
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs: [api.team.create.correlated]
negativeCases:
  - id: anonymous-denied
    actor: anonymous_user
    sceneManifest: ../scenes/negative/anonymous-team-access.scene.yaml
    executionKey: admin.team.negative.anonymous-access
    verifierRefs: [api.team.create.anonymous-denied]
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Create the durable team identity and membership boundary used by active-team selection and team management.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
