schemaVersion: treeseed.guarantee/v1
id: guarantee.team.team.switch-active-team.013
journeyIndex: 13
type: team
subtype: team
journey: Switch Active Team
ownerPackage: "@treeseed/admin"
summary: Change the active team context used by admin and API requests; must refresh project capacity and membership-scoped views.
status: active
dependencies:
  journeys:
    - 12
  guarantees:
    - guarantee.team.team.create-team.012
actors:
  allowed:
    - team_admin
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Planned by guarantee journey audit: the referenced UI route/behavior is not implemented as an executable service journey yet.
    - "Journey dependencies must be satisfied: 12"
scene:
  required: true
  manifest: ./scenes/switch-active-team.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/teams
  componentContract: inline-form/search-panel service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.team.list.owner
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: non-member-denied
    actor: non_member
    verifierRefs:
      - api.team.members.nonmember
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Change the active team context used by admin and API requests; must refresh project capacity and membership-scoped views.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
