schemaVersion: treeseed.guarantee/v1
id: guarantee.team.team.delete-team.016
journeyIndex: 16
type: team
subtype: team
journey: Delete Team
ownerPackage: "@treeseed/admin"
summary: Delete or archive a team only after checking projects hosts capacity and content ownership; likely requires explicit retention and transfer rules.
status: active
dependencies:
  journeys:
    - 12
  guarantees:
    - guarantee.team.team.create-team.012
actors:
  allowed:
    - team_admin
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Planned by guarantee journey audit: the referenced UI route/behavior is not implemented as an executable service journey yet.
    - "Journey dependencies must be satisfied: 12"
scene:
  required: true
  manifest: ./scenes/delete-team.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/teams/visual-audit-team/delete
  componentContract: destructive-confirmation service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.team.deletion-blockers
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: non-member-denied
    actor: non_member
    verifierRefs:
      - api.team.members.nonmember
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Delete or archive a team only after checking projects hosts capacity and content ownership; likely requires explicit retention and transfer rules.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
