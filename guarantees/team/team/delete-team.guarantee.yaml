schemaVersion: treeseed.guarantee/v1
id: guarantee.team.team.delete-team.016
journeyIndex: 16
type: team
subtype: team
journey: Delete Team
ownerPackage: "@treeseed/admin"
summary: Delete or archive a team only after checking projects hosts capacity and content ownership; likely requires explicit retention and transfer rules.
status: active
dependencies:
  journeys: []
  guarantees:
    - guarantee.team.membership.remove-team-member.020
    - guarantee.team.team.view-team-profile.015
actors:
  allowed:
    - team_admin
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Membership lifecycle and public profile verification must finish before deletion.
scene:
  required: true
  manifest: ../scenes/delete-team.scene.yaml
  executionKey: admin.team.delete
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/teams/[teamId]/delete
  componentContract: destructive-confirmation service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.team.deletion-blockers
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs: [api.team.lifecycle.audit]
negativeCases:
  - id: non-member-denied
    actor: non_member
    verifierRefs:
      - api.team.members.nonmember
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Delete or archive a team only after checking projects hosts capacity and content ownership; likely requires explicit retention and transfer rules.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
