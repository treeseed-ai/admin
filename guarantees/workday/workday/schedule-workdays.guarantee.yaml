schemaVersion: treeseed.guarantee/v1
id: guarantee.workday.workday.schedule-workdays.062
journeyIndex: 62
type: workday
subtype: workday
journey: Schedule Workdays
ownerPackage: "@treeseed/admin"
summary: Create duration and budget bounded workdays for projects with approved capacity; planning must cover eligible agents before repeats.
status: planned
dependencies:
  journeys:
    - 27
    - 28
    - 32
  guarantees:
    - guarantee.capacity.capacity.approve-capacity.027
    - guarantee.capacity.capacity.deploy-capacity.028
    - guarantee.project.project.create-project.032
actors:
  allowed:
    - project_manager
    - agent_operator
    - capacity_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 27, 28, 32"
scene:
  required: true
  manifest: ./scenes/schedule-workdays.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/workdays/schedule-workdays
  componentContract: dashboard/read-model guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.workday.workday.schedule-workdays.api
content:
  required: false
  verifierRefs:
    - todo.workday.workday.schedule-workdays.content
audit:
  required: true
  verifierRefs:
    - todo.workday.workday.schedule-workdays.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.workday.workday.schedule-workdays.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Create duration and budget bounded workdays for projects with approved capacity; planning must cover eligible agents before repeats.
