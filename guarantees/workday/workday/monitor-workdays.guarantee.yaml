schemaVersion: treeseed.guarantee/v1
id: guarantee.workday.workday.monitor-workdays.063
journeyIndex: 63
type: workday
subtype: workday
journey: Monitor Workdays
ownerPackage: "@treeseed/admin"
summary: Track live workday assignments mode runs generated artifacts usage and settlement; should continue scheduling useful planning work while budget and time remain.
status: planned
dependencies:
  journeys:
    - 27
    - 28
    - 32
  guarantees:
    - guarantee.capacity.capacity.approve-capacity.027
    - guarantee.capacity.capacity.deploy-capacity.028
    - guarantee.project.project.create-project.032
actors:
  allowed:
    - project_manager
    - agent_operator
    - capacity_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 27, 28, 32"
scene:
  required: true
  manifest: ./scenes/monitor-workdays.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/workdays/monitor-workdays
  componentContract: dashboard/read-model guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.workday.workday.monitor-workdays.api
content:
  required: false
  verifierRefs:
    - todo.workday.workday.monitor-workdays.content
audit:
  required: true
  verifierRefs:
    - todo.workday.workday.monitor-workdays.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.workday.workday.monitor-workdays.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Track live workday assignments mode runs generated artifacts usage and settlement; should continue scheduling useful planning work while budget and time remain.
