schemaVersion: treeseed.guarantee/v1
id: guarantee.marketplace-seller.listings.approve-listing.110
journeyIndex: 110
type: marketplace-seller
subtype: listings
journey: Approve Listing
ownerPackage: "@treeseed/admin"
summary: First-pass product guarantee for Approve Listing.
status: planned
dependencies:
  journeys:
    - 109
  guarantees:
    - guarantee.marketplace-seller.listings.submit-listing-for-review.109
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 109"
scene:
  required: true
  manifest: ./scenes/approve-listing.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/teams/:teamId/commerce/approve-listing
  componentContract: record-action guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.marketplace-seller.listings.approve-listing.api
content:
  required: false
  verifierRefs:
    - todo.marketplace-seller.listings.approve-listing.content
audit:
  required: true
  verifierRefs:
    - todo.marketplace-seller.listings.approve-listing.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.marketplace-seller.listings.approve-listing.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Approve Listing.
