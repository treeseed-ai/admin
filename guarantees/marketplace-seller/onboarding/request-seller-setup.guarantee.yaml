schemaVersion: treeseed.guarantee/v1
id: guarantee.marketplace-seller.onboarding.request-seller-setup.106
journeyIndex: 106
type: marketplace-seller
subtype: onboarding
journey: Request Seller Setup
ownerPackage: "@treeseed/admin"
summary: First-pass product guarantee for Request Seller Setup.
status: active
dependencies:
  journeys:
    - 12
  guarantees:
    - guarantee.team.team.create-team.012
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 12"
scene:
  required: true
  manifest: ./scenes/request-seller-setup.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/market/seller
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.marketplace-seller.onboarding.request-seller-setup.api
content:
  required: false
  verifierRefs:
    - admin.marketplace-seller.onboarding.request-seller-setup.content
audit:
  required: true
  verifierRefs:
    - admin.marketplace-seller.onboarding.request-seller-setup.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.marketplace-seller.onboarding.request-seller-setup.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Request Seller Setup.
