schemaVersion: treeseed.guarantee/v1
id: guarantee.project.project.destroy-project.036
journeyIndex: 36
type: project
subtype: project
journey: Destroy Project
ownerPackage: "@treeseed/admin"
summary: Destroy or archive a project with content retention provider assignment cleanup and TreeDX authorization cleanup; requires exact-state drift handling where infrastructure exists.
status: active
dependencies:
  journeys:
    - 32
  guarantees:
    - guarantee.project.project.create-project.032
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 32"
scene:
  required: true
  manifest: ./scenes/destroy-project.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/seed-project/delete
  componentContract: destructive-confirmation service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.project.destroy-project.api
content:
  required: false
  verifierRefs:
    - admin.project.project.destroy-project.content
audit:
  required: true
  verifierRefs:
    - admin.project.project.destroy-project.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.project.destroy-project.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Destroy or archive a project with content retention provider assignment cleanup and TreeDX authorization cleanup; requires exact-state drift handling where infrastructure exists.
