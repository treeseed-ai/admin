schemaVersion: treeseed.guarantee/v1
id: guarantee.project.project.view-project-profile.034
journeyIndex: 34
type: project
subtype: project
journey: View Project Profile
ownerPackage: "@treeseed/admin"
summary: Present project overview including agents objectives decisions content and capacity posture; should aggregate API and TreeDX-backed content references.
status: planned
dependencies:
  journeys:
    - 32
  guarantees:
    - guarantee.project.project.create-project.032
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 32"
scene:
  required: true
  manifest: ./scenes/view-project-profile.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/project/view-project-profile
  componentContract: dashboard/read-model guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.project.project.view-project-profile.api
content:
  required: false
  verifierRefs:
    - todo.project.project.view-project-profile.content
audit:
  required: true
  verifierRefs:
    - todo.project.project.view-project-profile.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.project.project.view-project-profile.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Present project overview including agents objectives decisions content and capacity posture; should aggregate API and TreeDX-backed content references.
