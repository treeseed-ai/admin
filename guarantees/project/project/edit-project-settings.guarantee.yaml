schemaVersion: treeseed.guarantee/v1
id: guarantee.project.project.edit-project-settings.033
journeyIndex: 33
type: project
subtype: project
journey: Edit Project Settings
ownerPackage: "@treeseed/admin"
summary: Update project metadata agents and content settings through API models; project manifests may inform defaults but runtime edits need durable records.
status: active
dependencies:
  journeys:
    - 32
  guarantees:
    - guarantee.project.project.create-project.032
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 32"
scene:
  required: true
  manifest: ./scenes/edit-project-settings.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/seed-project/settings
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.project.edit-project-settings.api
content:
  required: false
  verifierRefs:
    - admin.project.project.edit-project-settings.content
audit:
  required: true
  verifierRefs:
    - admin.project.project.edit-project-settings.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.project.edit-project-settings.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Update project metadata agents and content settings through API models; project manifests may inform defaults but runtime edits need durable records.
