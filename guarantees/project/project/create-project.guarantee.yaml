schemaVersion: treeseed.guarantee/v1
id: guarantee.project.project.create-project.032
journeyIndex: 32
type: project
subtype: project
journey: Create Project
ownerPackage: "@treeseed/admin"
summary: Create a Treeseed project owned by a team with content model and TreeDX routing metadata; should not depend on local package checkout state.
status: planned
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/create-project.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/project/create-project
  componentContract: full-page-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.project.project.create-project.api
content:
  required: false
  verifierRefs:
    - todo.project.project.create-project.content
audit:
  required: true
  verifierRefs:
    - todo.project.project.create-project.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.project.project.create-project.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Create a Treeseed project owned by a team with content model and TreeDX routing metadata; should not depend on local package checkout state.
