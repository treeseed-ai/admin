schemaVersion: treeseed.guarantee/v1
id: guarantee.project.knowledge.create-book-page.069
journeyIndex: 69
type: project
subtype: knowledge
journey: Create Book Page
ownerPackage: "@treeseed/admin"
summary: Create a knowledge page inside a book with required frontmatter and links; agent artifacts must be real Knowledge Hub content.
status: planned
dependencies:
  journeys:
    - 65
  guarantees:
    - guarantee.project.book.create-book.065
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 65"
scene:
  required: true
  manifest: ./scenes/create-book-page.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/knowledge/create-book-page
  componentContract: full-page-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.project.knowledge.create-book-page.api
content:
  required: true
  verifierRefs:
    - todo.project.knowledge.create-book-page.content
audit:
  required: true
  verifierRefs:
    - todo.project.knowledge.create-book-page.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.project.knowledge.create-book-page.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Create a knowledge page inside a book with required frontmatter and links; agent artifacts must be real Knowledge Hub content.
