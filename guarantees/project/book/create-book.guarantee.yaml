schemaVersion: treeseed.guarantee/v1
id: guarantee.project.book.create-book.065
journeyIndex: 65
type: project
subtype: book
journey: Create Book
ownerPackage: "@treeseed/admin"
summary: Create a Knowledge Hub book through the SDK content model and TreeDX; books organize project knowledge rather than serving as package docs only.
status: active
dependencies:
  journeys:
    - 32
  guarantees:
    - guarantee.project.project.create-project.032
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 32"
scene:
  required: true
  manifest: ./scenes/create-book.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/knowledge/books
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.book.create-book.api
content:
  required: true
  verifierRefs:
    - admin.project.book.create-book.content
audit:
  required: true
  verifierRefs:
    - admin.project.book.create-book.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.book.create-book.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Create a Knowledge Hub book through the SDK content model and TreeDX; books organize project knowledge rather than serving as package docs only.
