schemaVersion: treeseed.guarantee/v1
id: guarantee.project.book.search-books.068
journeyIndex: 68
type: project
subtype: book
journey: Search Books
ownerPackage: "@treeseed/admin"
summary: Search project books and metadata through content indexes; should not scan local fixture files in production workflows.
status: active
dependencies:
  journeys:
    - 65
  guarantees:
    - guarantee.project.book.create-book.065
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 65"
scene:
  required: true
  manifest: ./scenes/search-books.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/knowledge/books
  componentContract: inline-form/search-panel service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.book.search-books.api
content:
  required: true
  verifierRefs:
    - admin.project.book.search-books.content
audit:
  required: true
  verifierRefs:
    - admin.project.book.search-books.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.book.search-books.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Search project books and metadata through content indexes; should not scan local fixture files in production workflows.
