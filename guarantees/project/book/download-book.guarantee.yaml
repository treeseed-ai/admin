schemaVersion: treeseed.guarantee/v1
id: guarantee.project.book.download-book.070
journeyIndex: 70
type: project
subtype: book
journey: Download Book
ownerPackage: "@treeseed/admin"
summary: Export a book or selected pages from TreeDX-backed content; depends on an existing book and at least one available page.
status: active
dependencies:
  journeys:
    - 65
    - 69
  guarantees:
    - guarantee.project.book.create-book.065
    - guarantee.project.knowledge.create-book-page.069
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 65, 69"
scene:
  required: true
  manifest: ./scenes/download-book.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/knowledge/books/seed-book
  componentContract: inline-form/search-panel service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.book.download-book.api
content:
  required: true
  verifierRefs:
    - admin.project.book.download-book.content
audit:
  required: true
  verifierRefs:
    - admin.project.book.download-book.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.book.download-book.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Export a book or selected pages from TreeDX-backed content; depends on an existing book and at least one available page.
