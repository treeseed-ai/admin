schemaVersion: treeseed.guarantee/v1
id: guarantee.project.book.edit-book.066
journeyIndex: 66
type: project
subtype: book
journey: Edit Book
ownerPackage: "@treeseed/admin"
summary: Edit book metadata ordering and visibility through TreeDX-backed content operations.
status: planned
dependencies:
  journeys:
    - 65
  guarantees:
    - guarantee.project.book.create-book.065
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 65"
scene:
  required: true
  manifest: ./scenes/edit-book.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/book/edit-book
  componentContract: full-page-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.project.book.edit-book.api
content:
  required: true
  verifierRefs:
    - todo.project.book.edit-book.content
audit:
  required: true
  verifierRefs:
    - todo.project.book.edit-book.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.project.book.edit-book.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Edit book metadata ordering and visibility through TreeDX-backed content operations.
