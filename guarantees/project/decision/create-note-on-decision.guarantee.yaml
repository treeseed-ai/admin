schemaVersion: treeseed.guarantee/v1
id: guarantee.project.decision.create-note-on-decision.056
journeyIndex: 56
type: project
subtype: decision
journey: Create Note on Decision
ownerPackage: "@treeseed/admin"
summary: Attach notes to decisions using relatedDecisions or about frontmatter; should preserve why a decision was made.
status: active
dependencies:
  journeys:
    - 50
  guarantees:
    - guarantee.project.proposal.vote-on-proposal.050
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 50"
scene:
  required: true
  manifest: ./scenes/create-note-on-decision.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/work/decisions/seed-decision
  componentContract: inline-form/search-panel service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.decision.create-note-on-decision.api
content:
  required: true
  verifierRefs:
    - admin.project.decision.create-note-on-decision.content
audit:
  required: true
  verifierRefs:
    - admin.project.decision.create-note-on-decision.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.decision.create-note-on-decision.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Attach notes to decisions using relatedDecisions or about frontmatter; should preserve why a decision was made.
