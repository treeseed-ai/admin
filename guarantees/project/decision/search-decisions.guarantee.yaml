schemaVersion: treeseed.guarantee/v1
id: guarantee.project.decision.search-decisions.055
journeyIndex: 55
type: project
subtype: decision
journey: Search Decisions
ownerPackage: "@treeseed/admin"
summary: Search accepted or rejected decisions created from proposal review; decisions gate acting mode and capacity-plan provenance.
status: planned
dependencies:
  journeys:
    - 50
  guarantees:
    - guarantee.project.proposal.vote-on-proposal.050
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 50"
scene:
  required: true
  manifest: ./scenes/search-decisions.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/decision/search-decisions
  componentContract: inline-form/search-panel guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.project.decision.search-decisions.api
content:
  required: true
  verifierRefs:
    - todo.project.decision.search-decisions.content
audit:
  required: true
  verifierRefs:
    - todo.project.decision.search-decisions.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.project.decision.search-decisions.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Search accepted or rejected decisions created from proposal review; decisions gate acting mode and capacity-plan provenance.
