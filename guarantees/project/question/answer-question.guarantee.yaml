schemaVersion: treeseed.guarantee/v1
id: guarantee.project.question.answer-question.042
journeyIndex: 42
type: project
subtype: question
journey: Answer Question
ownerPackage: "@treeseed/admin"
summary: Create an answer or linked note with subject frontmatter; agent-generated answers must be traceable to execution and TreeDX context.
status: planned
dependencies:
  journeys:
    - 38
  guarantees:
    - guarantee.project.question.ask-question.038
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 38"
scene:
  required: true
  manifest: ./scenes/answer-question.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/question/answer-question
  componentContract: inline-form/search-panel guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.project.question.answer-question.api
content:
  required: true
  verifierRefs:
    - todo.project.question.answer-question.content
audit:
  required: true
  verifierRefs:
    - todo.project.question.answer-question.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.project.question.answer-question.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Create an answer or linked note with subject frontmatter; agent-generated answers must be traceable to execution and TreeDX context.
