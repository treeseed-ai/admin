schemaVersion: treeseed.guarantee/v1
id: guarantee.project.question.delete-question.040
journeyIndex: 40
type: project
subtype: question
journey: Delete Question
ownerPackage: "@treeseed/admin"
summary: Delete or archive a question through TreeDX with relationship checks; should avoid breaking linked notes decisions and proposals.
status: active
dependencies:
  journeys:
    - 38
  guarantees:
    - guarantee.project.question.ask-question.038
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 38"
scene:
  required: true
  manifest: ./scenes/delete-question.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/work/questions/seed-question
  componentContract: destructive-confirmation service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.question.delete-question.api
content:
  required: true
  verifierRefs:
    - admin.project.question.delete-question.content
audit:
  required: true
  verifierRefs:
    - admin.project.question.delete-question.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.question.delete-question.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Delete or archive a question through TreeDX with relationship checks; should avoid breaking linked notes decisions and proposals.
