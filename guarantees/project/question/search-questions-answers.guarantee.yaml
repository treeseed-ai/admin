schemaVersion: treeseed.guarantee/v1
id: guarantee.project.question.search-questions-answers.041
journeyIndex: 41
type: project
subtype: question
journey: Search Questions / Answers
ownerPackage: "@treeseed/admin"
summary: Search project questions and answers through indexed content APIs or TreeDX-backed query surfaces; missing search should be implemented as a content service capability.
status: active
dependencies:
  journeys:
    - 38
  guarantees:
    - guarantee.project.question.ask-question.038
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 38"
scene:
  required: true
  manifest: ./scenes/search-questions-answers.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/work/questions/seed-question
  componentContract: inline-form/search-panel service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.question.search-questions-answers.api
content:
  required: true
  verifierRefs:
    - admin.project.question.search-questions-answers.content
audit:
  required: true
  verifierRefs:
    - admin.project.question.search-questions-answers.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.question.search-questions-answers.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Search project questions and answers through indexed content APIs or TreeDX-backed query surfaces; missing search should be implemented as a content service capability.
