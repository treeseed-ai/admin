schemaVersion: treeseed.guarantee/v1
id: guarantee.project.question.edit-question.039
journeyIndex: 39
type: project
subtype: question
journey: Edit Question
ownerPackage: "@treeseed/admin"
summary: Edit question content through TreeDX with provenance and validation; should preserve links to answers notes proposals and workday context.
status: active
dependencies:
  journeys:
    - 38
  guarantees:
    - guarantee.project.question.ask-question.038
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 38"
scene:
  required: true
  manifest: ./scenes/edit-question.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/work/questions/seed-question/edit
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.question.edit-question.api
content:
  required: true
  verifierRefs:
    - admin.project.question.edit-question.content
audit:
  required: true
  verifierRefs:
    - admin.project.question.edit-question.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.question.edit-question.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Edit question content through TreeDX with provenance and validation; should preserve links to answers notes proposals and workday context.
