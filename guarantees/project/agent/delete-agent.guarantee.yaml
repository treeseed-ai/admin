schemaVersion: treeseed.guarantee/v1
id: guarantee.project.agent.delete-agent.059
journeyIndex: 59
type: project
subtype: agent
journey: Delete Agent
ownerPackage: "@treeseed/admin"
summary: Remove or disable an agent after checking scheduled workdays assignments and generated artifact ownership.
status: planned
dependencies:
  journeys:
    - 57
  guarantees:
    - guarantee.project.agent.create-agent.057
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 57"
scene:
  required: true
  manifest: ./scenes/delete-agent.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/agent/delete-agent
  componentContract: destructive-confirmation guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.project.agent.delete-agent.api
content:
  required: false
  verifierRefs:
    - todo.project.agent.delete-agent.content
audit:
  required: true
  verifierRefs:
    - todo.project.agent.delete-agent.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.project.agent.delete-agent.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Remove or disable an agent after checking scheduled workdays assignments and generated artifact ownership.
