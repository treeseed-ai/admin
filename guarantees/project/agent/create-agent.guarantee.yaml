schemaVersion: treeseed.guarantee/v1
id: guarantee.project.agent.create-agent.057
journeyIndex: 57
type: project
subtype: agent
journey: Create Agent
ownerPackage: "@treeseed/admin"
summary: Define a project-owned agent class handler prompts permissions and output contracts; providers supply execution capacity but do not define project agents.
status: active
dependencies:
  journeys:
    - 32
  guarantees:
    - guarantee.project.project.create-project.032
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 32"
scene:
  required: true
  manifest: ./scenes/create-agent.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/visual-audit-project/agents/new
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.agent.create-agent.api
content:
  required: false
  verifierRefs:
    - admin.project.agent.create-agent.content
audit:
  required: true
  verifierRefs:
    - admin.project.agent.create-agent.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.agent.create-agent.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Define a project-owned agent class handler prompts permissions and output contracts; providers supply execution capacity but do not define project agents.
