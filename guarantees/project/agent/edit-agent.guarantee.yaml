schemaVersion: treeseed.guarantee/v1
id: guarantee.project.agent.edit-agent.058
journeyIndex: 58
type: project
subtype: agent
journey: Edit Agent
ownerPackage: "@treeseed/admin"
summary: Edit agent configuration and permissions with validation against SDK/agent public contracts; changes should affect future assignments not mutate active runs unsafely.
status: active
dependencies:
  journeys:
    - 57
  guarantees:
    - guarantee.project.agent.create-agent.057
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 57"
scene:
  required: true
  manifest: ./scenes/edit-agent.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/seed-project/agents/seed-agent
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.agent.edit-agent.api
content:
  required: false
  verifierRefs:
    - admin.project.agent.edit-agent.content
audit:
  required: true
  verifierRefs:
    - admin.project.agent.edit-agent.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.agent.edit-agent.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Edit agent configuration and permissions with validation against SDK/agent public contracts; changes should affect future assignments not mutate active runs unsafely.
