schemaVersion: treeseed.guarantee/v1
id: guarantee.project.proposal.create-proposal.048
journeyIndex: 48
type: project
subtype: proposal
journey: Create Proposal
ownerPackage: "@treeseed/admin"
summary: Create a project proposal tied to objectives or questions; proposals should be content model records and ready for review or voting.
status: planned
dependencies:
  journeys:
    - 32
  guarantees:
    - guarantee.project.project.create-project.032
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 32"
scene:
  required: true
  manifest: ./scenes/create-proposal.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/proposal/create-proposal
  componentContract: full-page-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.project.proposal.create-proposal.api
content:
  required: true
  verifierRefs:
    - todo.project.proposal.create-proposal.content
audit:
  required: true
  verifierRefs:
    - todo.project.proposal.create-proposal.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.project.proposal.create-proposal.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Create a project proposal tied to objectives or questions; proposals should be content model records and ready for review or voting.
