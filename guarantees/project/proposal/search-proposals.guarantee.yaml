schemaVersion: treeseed.guarantee/v1
id: guarantee.project.proposal.search-proposals.052
journeyIndex: 52
type: project
subtype: proposal
journey: Search Proposals
ownerPackage: "@treeseed/admin"
summary: Search proposals by state objective agent and decision status; should power planning dashboards and review workflows.
status: active
dependencies:
  journeys:
    - 48
  guarantees:
    - guarantee.project.proposal.create-proposal.048
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 48"
scene:
  required: true
  manifest: ./scenes/search-proposals.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/work/proposals
  componentContract: inline-form/search-panel service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.proposal.search-proposals.api
content:
  required: true
  verifierRefs:
    - admin.project.proposal.search-proposals.content
audit:
  required: true
  verifierRefs:
    - admin.project.proposal.search-proposals.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.proposal.search-proposals.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Search proposals by state objective agent and decision status; should power planning dashboards and review workflows.
