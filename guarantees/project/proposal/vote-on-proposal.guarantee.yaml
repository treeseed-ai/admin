schemaVersion: treeseed.guarantee/v1
id: guarantee.project.proposal.vote-on-proposal.050
journeyIndex: 50
type: project
subtype: proposal
journey: Vote on Proposal
ownerPackage: "@treeseed/admin"
summary: Record review or approval decisions against proposals; approved decisions are required before acting mode can run.
status: planned
dependencies:
  journeys:
    - 48
  guarantees:
    - guarantee.project.proposal.create-proposal.048
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 48"
scene:
  required: true
  manifest: ./scenes/vote-on-proposal.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/proposal/vote-on-proposal
  componentContract: inline-form/search-panel guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.project.proposal.vote-on-proposal.api
content:
  required: true
  verifierRefs:
    - todo.project.proposal.vote-on-proposal.content
audit:
  required: true
  verifierRefs:
    - todo.project.proposal.vote-on-proposal.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.project.proposal.vote-on-proposal.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Record review or approval decisions against proposals; approved decisions are required before acting mode can run.
