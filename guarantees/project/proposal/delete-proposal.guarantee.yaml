schemaVersion: treeseed.guarantee/v1
id: guarantee.project.proposal.delete-proposal.051
journeyIndex: 51
type: project
subtype: proposal
journey: Delete Proposal
ownerPackage: "@treeseed/admin"
summary: Delete or archive proposals only after checking votes decisions and linked notes; destructive operations need audit trails.
status: active
dependencies:
  journeys:
    - 48
  guarantees:
    - guarantee.project.proposal.create-proposal.048
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 48"
scene:
  required: true
  manifest: ./scenes/delete-proposal.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/work/proposals/seed-proposal
  componentContract: destructive-confirmation service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.proposal.delete-proposal.api
content:
  required: true
  verifierRefs:
    - admin.project.proposal.delete-proposal.content
audit:
  required: true
  verifierRefs:
    - admin.project.proposal.delete-proposal.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.proposal.delete-proposal.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Delete or archive proposals only after checking votes decisions and linked notes; destructive operations need audit trails.
