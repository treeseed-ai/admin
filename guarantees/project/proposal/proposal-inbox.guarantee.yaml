schemaVersion: treeseed.guarantee/v1
id: guarantee.project.proposal.proposal-inbox.053
journeyIndex: 53
type: project
subtype: proposal
journey: Proposal Inbox
ownerPackage: "@treeseed/admin"
summary: Review pending proposals estimates and readiness signals; this surface is central for turning planning output into approved decisions.
status: planned
dependencies:
  journeys:
    - 48
  guarantees:
    - guarantee.project.proposal.create-proposal.048
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 48"
scene:
  required: true
  manifest: ./scenes/proposal-inbox.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/proposal/proposal-inbox
  componentContract: dashboard/read-model guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.project.proposal.proposal-inbox.api
content:
  required: true
  verifierRefs:
    - todo.project.proposal.proposal-inbox.content
audit:
  required: true
  verifierRefs:
    - todo.project.proposal.proposal-inbox.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.project.proposal.proposal-inbox.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Review pending proposals estimates and readiness signals; this surface is central for turning planning output into approved decisions.
