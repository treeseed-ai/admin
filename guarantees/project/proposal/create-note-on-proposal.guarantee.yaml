schemaVersion: treeseed.guarantee/v1
id: guarantee.project.proposal.create-note-on-proposal.054
journeyIndex: 54
type: project
subtype: proposal
journey: Create Note on Proposal
ownerPackage: "@treeseed/admin"
summary: Create linked proposal commentary with relatedProposals frontmatter; useful for reviews estimates and implementation notes.
status: active
dependencies:
  journeys:
    - 48
  guarantees:
    - guarantee.project.proposal.create-proposal.048
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 48"
scene:
  required: true
  manifest: ./scenes/create-note-on-proposal.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/work/proposals/seed-proposal
  componentContract: inline-form/search-panel service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.proposal.create-note-on-proposal.api
content:
  required: true
  verifierRefs:
    - admin.project.proposal.create-note-on-proposal.content
audit:
  required: true
  verifierRefs:
    - admin.project.proposal.create-note-on-proposal.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.proposal.create-note-on-proposal.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Create linked proposal commentary with relatedProposals frontmatter; useful for reviews estimates and implementation notes.
