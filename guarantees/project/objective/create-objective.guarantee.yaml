schemaVersion: treeseed.guarantee/v1
id: guarantee.project.objective.create-objective.043
journeyIndex: 43
type: project
subtype: objective
journey: Create Objective
ownerPackage: "@treeseed/admin"
summary: Create an objective in the project content model through TreeDX; objectives drive planning work and should be eligible for agent context.
status: active
dependencies:
  journeys:
    - 32
  guarantees:
    - guarantee.project.project.create-project.032
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 32"
scene:
  required: true
  manifest: ./scenes/create-objective.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/work/objectives/new
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.objective.create-objective.api
content:
  required: true
  verifierRefs:
    - admin.project.objective.create-objective.content
audit:
  required: true
  verifierRefs:
    - admin.project.objective.create-objective.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.objective.create-objective.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Create an objective in the project content model through TreeDX; objectives drive planning work and should be eligible for agent context.
