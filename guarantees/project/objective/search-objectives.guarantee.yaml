schemaVersion: treeseed.guarantee/v1
id: guarantee.project.objective.search-objectives.046
journeyIndex: 46
type: project
subtype: objective
journey: Search Objectives
ownerPackage: "@treeseed/admin"
summary: Search objectives by status ownership tags and related content; should use project-scoped content indexes rather than filesystem scans.
status: active
dependencies:
  journeys:
    - 43
  guarantees:
    - guarantee.project.objective.create-objective.043
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 43"
scene:
  required: true
  manifest: ./scenes/search-objectives.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/work/objectives
  componentContract: inline-form/search-panel service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.objective.search-objectives.api
content:
  required: true
  verifierRefs:
    - admin.project.objective.search-objectives.content
audit:
  required: true
  verifierRefs:
    - admin.project.objective.search-objectives.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.objective.search-objectives.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Search objectives by status ownership tags and related content; should use project-scoped content indexes rather than filesystem scans.
