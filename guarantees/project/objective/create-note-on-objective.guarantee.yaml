schemaVersion: treeseed.guarantee/v1
id: guarantee.project.objective.create-note-on-objective.047
journeyIndex: 47
type: project
subtype: objective
journey: Create Note on Objective
ownerPackage: "@treeseed/admin"
summary: Create a linked note with relatedObjectives or about frontmatter; this is a canonical planning artifact for workdays.
status: active
dependencies:
  journeys:
    - 43
  guarantees:
    - guarantee.project.objective.create-objective.043
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 43"
scene:
  required: true
  manifest: ./scenes/create-note-on-objective.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/work/objectives/seed-objective
  componentContract: inline-form/search-panel service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.objective.create-note-on-objective.api
content:
  required: true
  verifierRefs:
    - admin.project.objective.create-note-on-objective.content
audit:
  required: true
  verifierRefs:
    - admin.project.objective.create-note-on-objective.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.objective.create-note-on-objective.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Create a linked note with relatedObjectives or about frontmatter; this is a canonical planning artifact for workdays.
