schemaVersion: treeseed.guarantee/v1
id: guarantee.project.objective.edit-objective.044
journeyIndex: 44
type: project
subtype: objective
journey: Edit Objective
ownerPackage: "@treeseed/admin"
summary: Edit objective fields and status while preserving links to proposals decisions notes and workday artifacts.
status: planned
dependencies:
  journeys:
    - 43
  guarantees:
    - guarantee.project.objective.create-objective.043
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 43"
scene:
  required: true
  manifest: ./scenes/edit-objective.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/projects/:projectId/objective/edit-objective
  componentContract: full-page-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.project.objective.edit-objective.api
content:
  required: true
  verifierRefs:
    - todo.project.objective.edit-objective.content
audit:
  required: true
  verifierRefs:
    - todo.project.objective.edit-objective.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.project.objective.edit-objective.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Edit objective fields and status while preserving links to proposals decisions notes and workday artifacts.
