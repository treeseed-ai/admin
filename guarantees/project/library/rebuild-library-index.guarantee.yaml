schemaVersion: treeseed.guarantee/v1
id: guarantee.project.library.rebuild-library-index.090
journeyIndex: 90
type: project
subtype: library
journey: Rebuild Library Index
ownerPackage: "@treeseed/admin"
summary: Refresh searchable project content indexes after book page or note changes; supports search and export journeys without filesystem scans.
status: active
dependencies:
  journeys:
    - 65
    - 69
  guarantees:
    - guarantee.project.book.create-book.065
    - guarantee.project.knowledge.create-book-page.069
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 65, 69"
scene:
  required: true
  manifest: ./scenes/rebuild-library-index.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/knowledge
  componentContract: record-action service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.library.rebuild-library-index.api
content:
  required: true
  verifierRefs:
    - admin.project.library.rebuild-library-index.content
audit:
  required: true
  verifierRefs:
    - admin.project.library.rebuild-library-index.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.library.rebuild-library-index.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Refresh searchable project content indexes after book page or note changes; supports search and export journeys without filesystem scans.
