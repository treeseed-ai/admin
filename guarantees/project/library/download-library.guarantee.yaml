schemaVersion: treeseed.guarantee/v1
id: guarantee.project.library.download-library.073
journeyIndex: 73
type: project
subtype: library
journey: Download Library
ownerPackage: "@treeseed/admin"
summary: Export the project knowledge library from TreeDX-backed content; should honor project authorization and may need dependencies on books pages and library indexing.
status: active
dependencies:
  journeys:
    - 69
  guarantees:
    - guarantee.project.knowledge.create-book-page.069
actors:
  allowed:
    - project_manager
    - project_contributor
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 69"
scene:
  required: true
  manifest: ./scenes/download-library.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/knowledge
  componentContract: inline-form/search-panel service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.project.library.download-library.api
content:
  required: true
  verifierRefs:
    - admin.project.library.download-library.content
audit:
  required: true
  verifierRefs:
    - admin.project.library.download-library.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.project.library.download-library.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Export the project knowledge library from TreeDX-backed content; should honor project authorization and may need dependencies on books pages and library indexing.
