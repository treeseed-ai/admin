schemaVersion: treeseed.guarantee/v1
id: guarantee.work.agent-lab.monitor-agent-lab.035
journeyIndex: 741
type: work
subtype: agent-lab
journey: "Monitor the team Agent Lab"
ownerPackage: "@treeseed/admin"
surface: admin-ui
summary: "A team member sees one compact, responsive, revisioned Agent Lab and can inspect an agent as designed intent, assignment-time state, or observed forensic activity; the anatomy workbench edits only human intent while generated identity, class linkage, repository placement, and operational status remain locked system facts."
status: planned
dependencies: { journeys: [738], guarantees: [guarantee.work.command.monitor-team-operations.033] }
actors: { allowed: [authenticated_user, team_owner, project_lead], forbidden: [unauthorized_user] }
devices: { required: [desktop_chromium, mobile_chromium] }
gates: [future]
preconditions: { fixtures: [integrated_treeseed_project], notes: ["An active team is selected and the API projection is available."] }
api: { required: false, verifierRefs: [] }
content: { required: false, verifierRefs: [] }
audit: { required: false, verifierRefs: [] }
negativeCases: ["Snapshot mode makes no background requests.", "Closed charts do not poll.", "Stale responses and cursor corrections cannot restore removed records.", "Generated identity and runtime status cannot be edited as design intent.", "Provider supply cannot be widened from an agent definition.", "Polling does not reset the selected anatomy region or close cascading note and question overlays.", "Advanced forensic fields never displace the primary human-readable content by default."]
evidence: { required: [guarantee_report] }
notes: ["Planned while the accelerated anatomy design is iterated; activate after light/dark, idle/running/waiting/degraded, narrow, keyboard navigation, intent compilation, conflict, and TreeDX commit scenes produce repeatable evidence."]
