schemaVersion: treeseed.guarantee/v1
id: guarantee.host.secrets.reset-passphrase.022
journeyIndex: 22
type: host
subtype: secrets
journey: Reset Passphrase
ownerPackage: "@treeseed/admin"
summary: Reset secret access with recovery policy and re-encryption semantics; this is high risk because provider credentials back reconciliation.
status: active
dependencies:
  journeys:
    - 21
  guarantees:
    - guarantee.host.secrets.create-passphrase.021
actors:
  allowed:
    - host_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 21"
scene:
  required: true
  manifest: ./scenes/reset-passphrase.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/hosts/web/seed-host/settings
  componentContract: modal-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.host.secrets.reset-passphrase.api
content:
  required: false
  verifierRefs:
    - admin.host.secrets.reset-passphrase.content
audit:
  required: true
  verifierRefs:
    - admin.host.secrets.reset-passphrase.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.host.secrets.reset-passphrase.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Reset secret access with recovery policy and re-encryption semantics; this is high risk because provider credentials back reconciliation.
