schemaVersion: treeseed.guarantee/v1
id: guarantee.host.secrets.create-passphrase.021
journeyIndex: 21
type: host
subtype: secrets
journey: Create Passphrase
ownerPackage: "@treeseed/admin"
summary: Create or unlock the secret-management passphrase for host/provider credentials; secrets must remain in approved secret stores and never plaintext files.
status: planned
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - host_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/create-passphrase.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/hosts/create-passphrase
  componentContract: modal-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.host.secrets.create-passphrase.api
content:
  required: false
  verifierRefs:
    - todo.host.secrets.create-passphrase.content
audit:
  required: true
  verifierRefs:
    - todo.host.secrets.create-passphrase.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.host.secrets.create-passphrase.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Create or unlock the secret-management passphrase for host/provider credentials; secrets must remain in approved secret stores and never plaintext files.
