schemaVersion: treeseed.guarantee/v1
id: guarantee.host.host.edit-host.024
journeyIndex: 24
type: host
subtype: host
journey: Edit Host
ownerPackage: "@treeseed/admin"
summary: Edit host configuration such as domains provider bindings and environment metadata; changes must flow through trsd reconciliation plans.
status: active
dependencies:
  journeys:
    - 23
  guarantees:
    - guarantee.host.host.create-host.023
actors:
  allowed:
    - host_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 23"
scene:
  required: true
  manifest: ./scenes/edit-host.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/hosts
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.host.host.edit-host.api
content:
  required: false
  verifierRefs:
    - admin.host.host.edit-host.content
audit:
  required: true
  verifierRefs:
    - admin.host.host.edit-host.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.host.host.edit-host.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Edit host configuration such as domains provider bindings and environment metadata; changes must flow through trsd reconciliation plans.
