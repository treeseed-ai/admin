schemaVersion: treeseed.guarantee/v1
id: guarantee.host.host.create-host.023
journeyIndex: 23
type: host
subtype: host
journey: Create Host
ownerPackage: "@treeseed/admin"
summary: Register a hostable environment or provider target as desired state; creation should compile reconciliation resources instead of calling providers directly.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - host_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/create-host.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/hosts/new
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.host.host.create-host.api
content:
  required: false
  verifierRefs:
    - admin.host.host.create-host.content
audit:
  required: true
  verifierRefs:
    - admin.host.host.create-host.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.host.host.create-host.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Register a hostable environment or provider target as desired state; creation should compile reconciliation resources instead of calling providers directly.
