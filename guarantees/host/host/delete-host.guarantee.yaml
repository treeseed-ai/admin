schemaVersion: treeseed.guarantee/v1
id: guarantee.host.host.delete-host.025
journeyIndex: 25
type: host
subtype: host
journey: Delete Host
ownerPackage: "@treeseed/admin"
summary: Plan destroy or retention for owned resources using reconciliation lifecycle; direct provider deletion would violate the architecture.
status: active
dependencies:
  journeys:
    - 23
  guarantees:
    - guarantee.host.host.create-host.023
actors:
  allowed:
    - host_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 23"
scene:
  required: true
  manifest: ./scenes/delete-host.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/hosts/web/seed-host
  componentContract: destructive-confirmation service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.host.host.delete-host.api
content:
  required: false
  verifierRefs:
    - admin.host.host.delete-host.content
audit:
  required: true
  verifierRefs:
    - admin.host.host.delete-host.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.host.host.delete-host.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Plan destroy or retention for owned resources using reconciliation lifecycle; direct provider deletion would violate the architecture.
