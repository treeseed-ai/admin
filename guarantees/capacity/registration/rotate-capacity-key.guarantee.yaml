schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity.registration.rotate-capacity-key.026
journeyIndex: 26
type: capacity
subtype: registration
journey: Rotate Capacity Key
ownerPackage: "@treeseed/admin"
summary: Rotate provider API keys used by capacity providers; the API should issue and track keys without exposing raw credentials in the UI.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - capacity_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/rotate-capacity-key.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/capacity/providers/seed-provider/keys
  componentContract: inline-form/search-panel service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.capacity.registration.rotate-capacity-key.api
content:
  required: false
  verifierRefs:
    - admin.capacity.registration.rotate-capacity-key.content
audit:
  required: true
  verifierRefs:
    - admin.capacity.registration.rotate-capacity-key.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.capacity.registration.rotate-capacity-key.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Rotate provider API keys used by capacity providers; the API should issue and track keys without exposing raw credentials in the UI.
