schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity.capacity.monitor-capacity.029
journeyIndex: 29
type: capacity
subtype: capacity
journey: Monitor Capacity
ownerPackage: "@treeseed/admin"
summary: Show provider manager runner availability leases usage and diagnostics; data should come from API control-plane records and provider check-ins.
status: planned
dependencies:
  journeys:
    - 27
    - 28
  guarantees:
    - guarantee.capacity.capacity.approve-capacity.027
    - guarantee.capacity.capacity.deploy-capacity.028
actors:
  allowed:
    - capacity_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 27, 28"
scene:
  required: true
  manifest: ./scenes/monitor-capacity.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/capacity/monitor-capacity
  componentContract: dashboard/read-model guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.capacity.capacity.monitor-capacity.api
content:
  required: false
  verifierRefs:
    - todo.capacity.capacity.monitor-capacity.content
audit:
  required: true
  verifierRefs:
    - todo.capacity.capacity.monitor-capacity.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.capacity.capacity.monitor-capacity.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Show provider manager runner availability leases usage and diagnostics; data should come from API control-plane records and provider check-ins.
