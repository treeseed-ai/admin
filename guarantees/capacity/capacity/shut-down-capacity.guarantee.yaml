schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity.capacity.shut-down-capacity.030
journeyIndex: 30
type: capacity
subtype: capacity
journey: Shut Down Capacity
ownerPackage: "@treeseed/admin"
summary: Stop provider runtime through reconciliation while preserving volumes logs and state as required; should not manually kill provider resources.
status: active
dependencies:
  journeys:
    - 28
  guarantees:
    - guarantee.capacity.capacity.deploy-capacity.028
actors:
  allowed:
    - capacity_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 28"
scene:
  required: true
  manifest: ./scenes/shut-down-capacity.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/capacity/runtime
  componentContract: destructive-confirmation service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.capacity.capacity.shut-down-capacity.api
content:
  required: false
  verifierRefs:
    - admin.capacity.capacity.shut-down-capacity.content
audit:
  required: true
  verifierRefs:
    - admin.capacity.capacity.shut-down-capacity.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.capacity.capacity.shut-down-capacity.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Stop provider runtime through reconciliation while preserving volumes logs and state as required; should not manually kill provider resources.
