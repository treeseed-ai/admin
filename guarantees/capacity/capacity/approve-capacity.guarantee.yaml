schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity.capacity.approve-capacity.027
journeyIndex: 27
type: capacity
subtype: capacity
journey: Approve Capacity
ownerPackage: "@treeseed/admin"
summary: Approve provider availability or registration for team/project use; API owns durable availability sessions and policy decisions.
status: planned
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - capacity_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/approve-capacity.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/capacity/approve-capacity
  componentContract: record-action guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.capacity.capacity.approve-capacity.api
content:
  required: false
  verifierRefs:
    - todo.capacity.capacity.approve-capacity.content
audit:
  required: true
  verifierRefs:
    - todo.capacity.capacity.approve-capacity.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.capacity.capacity.approve-capacity.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Approve provider availability or registration for team/project use; API owns durable availability sessions and policy decisions.
