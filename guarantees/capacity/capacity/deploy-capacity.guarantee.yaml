schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity.capacity.deploy-capacity.028
journeyIndex: 28
type: capacity
subtype: capacity
journey: Deploy Capacity
ownerPackage: "@treeseed/admin"
summary: Start or reconcile provider runtime through the agent package lifecycle and trsd capacity commands; UI should trigger desired-state operations not bespoke deployment scripts.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - capacity_operator
    - team_owner
    - platform_operator
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - desktop_firefox
    - tablet_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/deploy-capacity.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/capacity/runtime
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - admin.capacity.capacity.deploy-capacity.api
content:
  required: false
  verifierRefs:
    - admin.capacity.capacity.deploy-capacity.content
audit:
  required: true
  verifierRefs:
    - admin.capacity.capacity.deploy-capacity.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - admin.capacity.capacity.deploy-capacity.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - Start or reconcile provider runtime through the agent package lifecycle and trsd capacity commands; UI should trigger desired-state operations not bespoke deployment scripts.
