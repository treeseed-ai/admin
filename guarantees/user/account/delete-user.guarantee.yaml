schemaVersion: treeseed.guarantee/v1
id: guarantee.user.account.delete-user.011
journeyIndex: 11
type: user
subtype: account
journey: Delete User
ownerPackage: "@treeseed/admin"
summary: Perform a destructive account removal with ownership and membership checks; needs policy around teams projects
  content and provider-owned auth records.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/delete-user.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/account/delete-user
  componentContract: destructive-confirmation guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.auth.account.blockers
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: anonymous-denied
    actor: anonymous_user
    verifierRefs:
      - api.auth.me.requires-user
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Perform a destructive account removal with ownership and membership checks; needs policy around teams projects
    content and provider-owned auth records.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
