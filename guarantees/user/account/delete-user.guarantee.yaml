schemaVersion: treeseed.guarantee/v1
id: guarantee.user.account.delete-user.011
journeyIndex: 11
type: user
subtype: account
journey: Delete User
ownerPackage: "@treeseed/admin"
summary: Perform a destructive account removal with ownership and membership checks; needs policy around teams projects content and provider-owned auth records.
status: active
dependencies:
  journeys: []
  guarantees:
    - guarantee.user.auth.user-logout.005
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Team deletion and logout must complete before final account deletion.
scene:
  required: true
  manifest: ./scenes/delete-user.scene.yaml
  executionKey: admin.account.delete
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/account/delete
  componentContract: destructive-confirmation service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.auth.account.blockers
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs: [api.auth.lifecycle.audit]
negativeCases:
  - id: anonymous-denied
    actor: anonymous_user
    verifierRefs:
      - api.auth.me.requires-user
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Perform a destructive account removal with ownership and membership checks; needs policy around teams projects content and provider-owned auth records.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
