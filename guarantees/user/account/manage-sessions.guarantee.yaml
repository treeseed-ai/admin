schemaVersion: treeseed.guarantee/v1
id: guarantee.user.account.manage-sessions.007
journeyIndex: 7
type: user
subtype: account
journey: Manage Sessions
ownerPackage: "@treeseed/admin"
summary: List and revoke active sessions through the auth/session facade; this may be missing if the selected auth
  provider does not expose session management yet.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/manage-sessions.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/account/manage-sessions
  componentContract: record-operations guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.auth.sessions
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: anonymous-denied
    actor: anonymous_user
    verifierRefs:
      - api.auth.me.requires-user
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - List and revoke active sessions through the auth/session facade; this may be missing if the selected auth provider
    does not expose session management yet.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
