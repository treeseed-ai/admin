schemaVersion: treeseed.guarantee/v1
id: guarantee.user.account.manage-sessions.007
journeyIndex: 7
type: user
subtype: account
journey: Manage Sessions
ownerPackage: "@treeseed/admin"
summary: List active sessions with source IP and account-time-zone-aware activity dates, revoke a non-current session, and preserve the current session through the auth/session facade.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - The acceptance fixture supplies a third revocable session while preserving both journey sessions.
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/manage-sessions.scene.yaml
  executionKey: admin.account.sessions
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/account/sessions
  componentContract: record-operations service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.auth.sessions
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs: [api.auth.lifecycle.audit]
negativeCases:
  - id: anonymous-denied
    actor: anonymous_user
    verifierRefs:
      - api.auth.me.requires-user
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Session metadata records the trusted forwarded client address at authentication and presents an explicit fallback when an address was unavailable.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
