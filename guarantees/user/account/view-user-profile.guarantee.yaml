schemaVersion: treeseed.guarantee/v1
id: guarantee.user.account.view-user-profile.010
journeyIndex: 10
type: user
subtype: account
journey: View User Profile
ownerPackage: "@treeseed/admin"
summary: Show public user identity, expertise, and explicitly attributed reusable knowledge, with canonical account tabs only for the owning signed-in user.
status: active
dependencies:
  journeys:
    - 6
  guarantees:
    - guarantee.user.account.edit-account-settings.006
actors:
  allowed:
    - verified_user
    - anonymous_user
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - The authenticated owning user sees canonical account-management tabs above the profile.
    - Anonymous redaction remains covered by the public API verifier.
    - "Journey dependencies must be satisfied: 1"
scene:
  required: true
  manifest: ./scenes/view-user-profile.scene.yaml
  executionKey: admin.account.public-profile
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /u/[username]
  componentContract: public knowledge profile
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.auth.public-knowledge-profile
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: private-context-redacted
    actor: anonymous_user
    verifierRefs:
      - api.auth.public-knowledge-profile
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Public activity is based only on explicit public attribution; team membership never implies authorship.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
