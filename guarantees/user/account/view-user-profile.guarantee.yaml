schemaVersion: treeseed.guarantee/v1
id: guarantee.user.account.view-user-profile.010
journeyIndex: 10
type: user
subtype: account
journey: View User Profile
ownerPackage: "@treeseed/admin"
summary: Show public user identity and profile metadata without project, catalog, or knowledge distribution details.
status: planned
dependencies:
  journeys:
    - 1
  guarantees:
    - guarantee.user.auth.register-user.001
actors:
  allowed:
    - verified_user
    - anonymous_user
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - The profile scene follows the authenticated account page's canonical public-profile link.
    - "Journey dependencies must be satisfied: 1"
scene:
  required: true
  manifest: ./scenes/view-user-profile.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/account
  componentContract: public identity profile
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.auth.me.site-admin
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: anonymous-denied
    actor: anonymous_user
    verifierRefs:
      - api.auth.me.requires-user
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Public user profiles are identity-only during the redesign foundation phase.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: false
