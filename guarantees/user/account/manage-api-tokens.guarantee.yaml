schemaVersion: treeseed.guarantee/v1
id: guarantee.user.account.manage-api-tokens.094
journeyIndex: 94
type: user
subtype: account
journey: Manage API Tokens
ownerPackage: "@treeseed/admin"
summary: Create rotate and revoke user or automation tokens where supported; should be separate from provider capacity keys.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - Planned by guarantee journey audit: the referenced UI route/behavior is not implemented as an executable service journey yet.
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/manage-api-tokens.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/account
  componentContract: record-operations service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.auth.sessions
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: anonymous-denied
    actor: anonymous_user
    verifierRefs:
      - api.auth.me.requires-user
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Create rotate and revoke user or automation tokens where supported; should be separate from provider capacity keys.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
