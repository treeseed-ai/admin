schemaVersion: treeseed.guarantee/v1
id: guarantee.user.account.edit-account-settings.006
journeyIndex: 6
type: user
subtype: account
journey: Edit Account Settings
ownerPackage: "@treeseed/admin"
summary: Edit user profile preferences owned by the admin/account surface; persistence should live in the API or auth
  metadata and not in root Market-only state.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/edit-account-settings.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/account/edit-account-settings
  componentContract: full-page-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.auth.profile.update
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: anonymous-denied
    actor: anonymous_user
    verifierRefs:
      - api.auth.me.requires-user
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Edit user profile preferences owned by the admin/account surface; persistence should live in the API or auth
    metadata and not in root Market-only state.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
