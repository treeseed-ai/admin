schemaVersion: treeseed.guarantee/v1
id: guarantee.user.account.edit-account-settings.006
journeyIndex: 6
type: user
subtype: account
journey: Edit Account Settings
ownerPackage: "@treeseed/admin"
summary: Edit user identity, verify additional account emails, and persist the account-wide time zone used by every UI timestamp.
status: active
dependencies:
  journeys:
    - 4
  guarantees:
    - guarantee.user.auth.user-login.004
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - The onboarding identity supplies the stable username used by the anonymous profile proof.
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/edit-account-settings.scene.yaml
  executionKey: admin.account.edit-profile
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/account
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.auth.profile.update
    - api.auth.account-preferences
    - api.auth.email.lifecycle
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - api.auth.lifecycle.audit
negativeCases:
  - id: anonymous-denied
    actor: anonymous_user
    verifierRefs:
      - api.auth.me.requires-user
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Account identity, additional verified emails, and the selected IANA time zone persist through API-owned records; notification delivery consumes the same account time zone.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
