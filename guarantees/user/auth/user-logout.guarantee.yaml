schemaVersion: treeseed.guarantee/v1
id: guarantee.user.auth.user-logout.005
journeyIndex: 5
type: user
subtype: auth
journey: User Logout
ownerPackage: "@treeseed/admin"
summary: Revoke this application session and clear its host-only cookies without ending another application's identity session.
status: active
dependencies:
  journeys: []
  guarantees:
    - guarantee.user.account.view-user-profile.010
    - guarantee.user.account.manage-sessions.007
    - guarantee.user.account.manage-notifications.008
    - guarantee.user.account.manage-appearance.009
    - guarantee.team.team.delete-team.016
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
  optional:
    - mobile_webkit
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 4"
scene:
  required: true
  manifest: ./scenes/user-logout.scene.yaml
  executionKey: admin.identity.logout
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /app/
  componentContract: redirect/session guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.auth.sessions
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs: [api.auth.lifecycle.audit]
negativeCases:
  - id: anonymous-denied
    actor: anonymous_user
    verifierRefs:
      - api.auth.me.requires-user
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - POST /auth/logout requires CSRF and matching Origin, revokes the opaque application handle, and returns to the public application root.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
