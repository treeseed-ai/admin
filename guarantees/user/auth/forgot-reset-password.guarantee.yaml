schemaVersion: treeseed.guarantee/v1
id: guarantee.user.auth.forgot-reset-password.003
journeyIndex: 3
type: user
subtype: auth
journey: Forgot / Reset Password
ownerPackage: "@treeseed/admin"
summary: Request and complete password reset through the auth provider; missing pieces should be handled as auth
  integration work instead of custom user-table mutation.
status: active
dependencies:
  journeys:
    - 1
  guarantees:
    - guarantee.user.auth.register-user.001
actors:
  allowed:
    - anonymous_user
  forbidden:
    - already_authenticated_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
    - mobile_webkit
gates:
  - core
  - release
preconditions:
  fixtures: []
  notes:
    - "Journey dependencies must be satisfied: 1"
scene:
  required: true
  manifest: ./scenes/forgot-reset-password.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /auth/forgot-password
  componentContract: auth-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - api.auth.password-reset.request.mailpit
content:
  required: false
  verifierRefs: []
audit:
  required: false
  verifierRefs: []
negativeCases:
  - id: anonymous-denied
    actor: anonymous_user
    verifierRefs:
      - api.auth.me.requires-user
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
  optional:
    - mailpit_message
notes:
  - Request and complete password reset through the auth provider; missing pieces should be handled as auth integration
    work instead of custom user-table mutation.
surface: admin-ui
run:
  timeoutSeconds: 300
  requiredForRelease: true
