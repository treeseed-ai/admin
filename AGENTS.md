# Admin workspace guidance

Preserve the independent Admin package build and access-controlled project knowledge surfaces.

## Project library

Use `trsd library show admin` and `status` before querying `treeseed-ai/admin-library`. Read root-level paths at an exact commit. Author only through governed library workspaces and reviews. Never recreate a library `src/content` tree or edit `.treeseed/data` directly.
