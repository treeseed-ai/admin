---
import SettingsTemplate from '@treeseed/ui/components/astro/templates/SettingsTemplate.astro';
import EmptyState from '@treeseed/ui/components/astro/surface/EmptyState.astro';
import DirectionContentForm from '../../../../components/work/DirectionContentForm.astro';
import TreeseedAppLayout from '../../../../layouts/TreeseedAppLayout.astro';
import { loadDirectionCollection } from '../../../../view-models/operating-loop.vm.js';

if (!Astro.locals.auth?.principal) return Astro.redirect(`/auth/sign-in?returnTo=${encodeURIComponent('/app/work/notes/new')}`);
const vm = await loadDirectionCollection(Astro, 'notes', Astro.url);
const selectedProjectId = String(Astro.url.searchParams.get('projectId') ?? vm.context.projects[0]?.id ?? '');
---

<TreeseedAppLayout title="New note" description="Create one project note." currentPath="/app/work/notes" teams={vm.context.teams} activeTeam={vm.context.activeTeam} helpContext={vm.helpContext} feedbackContext={vm.feedbackContext}>
	{vm.context.projects.length ? (
		<SettingsTemplate title="New note" description="Create a note through the local content operation queue." actions={vm.actions}>
			<DirectionContentForm collection="notes" mode="create" projectOptions={vm.context.projects.map((project: any) => ({ label: project.name ?? project.slug, value: project.id }))} selectedProjectId={selectedProjectId} fallbackHref="/app/work/notes" />
		</SettingsTemplate>
	) : (
		<EmptyState title="No projects" description="Create a project before creating notes." actions={[{ label: 'New project', href: '/app/projects/new' }]} />
	)}
</TreeseedAppLayout>
